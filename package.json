{
  "name": "langraph-marketing-workshop",
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "deploy": "slidev build --base /langraph-workshop/ && gh-pages -d dist"
  },
  "devDependencies": {
    "@slidev/cli": "^52.14.1",
    "@slidev/theme-default": "^0.25.0",
    "gh-pages": "^6.3.0"
  }
}
